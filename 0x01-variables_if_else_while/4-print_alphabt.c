#include <stdio.h>
/**
 * main - Entry point
 *print alphabet except q and e
 *Return: is always 0
 */

int main(void)
{
	char c;
	{

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	}
	if (c == 'q' && 'e')
	continue;

	putchar('\n');
	return (0);
}
