#include "main.h"
/**
 *_isdigit - a function that checks for a digit
 *@c: assign a value to it
 *Return: is always 0
 */

int _isdigit(int c)
{
	if (c >= 0 && c <= 9, c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
