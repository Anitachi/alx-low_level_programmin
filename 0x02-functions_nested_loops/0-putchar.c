#include "main.h"
/**
 * main - a program that prints putchar
 * Return: is always 0
 */

int main(void)
{
	int = get_int(_putchar;)

		int i;

	for (int i = 0; i++)
	{
		write("_putchar %i\n", i);
	}
	return (0);
}
