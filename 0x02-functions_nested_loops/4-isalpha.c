#include "main.h"

/**
 * _isalpha - checks for alphabetic character
 * @c: a valuse assigned to c
 * Return: rturn 1 if c is a letter else return 0
 */

int _isalpha(int c)
{
	if (c >= 'a' || c <= 'z')
		if (c >= 'A' || c >= 'Z')
	{
		return (1);
	}
		else
		{
			return (0);
		}
}
